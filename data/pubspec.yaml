name: data
description: Module represent data layer.
version: 1.0.0+1

publish_to: 'none'

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  core:
    path: ../core
  dio: ^5.4.3+1
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  json_annotation: ^4.8.0
  retrofit: ^4.1.0
  talker_dio_logger: ^4.1.3

dev_dependencies:
  build_runner: ^2.4.9
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.1
  retrofit_generator: ^8.1.0
